<!DOCTYPE html>
<html>
<head>
    <title>TOTP Authentication</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .totp-container {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        .totp-form input {
            padding: 10px;
            margin-top: 10px;
            border-radius: 4px;
            border: 1px solid #ddd;
            width: calc(100% - 22px);
        }

        .totp-form button {
            padding: 10px 20px;
            margin-top: 20px;
            border: none;
            border-radius: 4px;
            background-color: #007bff;
            color: white;
            cursor: pointer;
        }

        .totp-form button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="totp-container">
        <h2>TOTP Authentication</h2>
        <p>Please enter your TOTP code(6-digit number).</p>
        <form action="/admin" method="post" class="totp-form">
            <input type="text" id="totp" name="token" placeholder="TOTP Code" required="">
            <button type="submit">Authenticate</button>
        </form>
    </div>
</body>
</html>