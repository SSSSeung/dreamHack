<!DOCTYPE html>
<html>
<head>
    <title>My Page</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .mypage-container {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 8px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #f4f4f4;
        }

        .input-field {
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 5px;
            border-radius: 4px;
            border: 1px solid #ddd;
            width: calc(100% - 16px);
        }

        .edit-button {
            padding: 10px 20px;
            margin-top: 20px;
            border: none;
            border-radius: 4px;
            background-color: #007bff;
            color: white;
            cursor: pointer;
        }

        .edit-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="mypage-container">
        <h2>My Page</h2>
        <form action="/mypage" method="post">
            <table>
                <tr>
                    <th>Username</th>
                    <td><%= username %></td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td><input type="text" name="name" class="input-field" value="<%= name %>"></td>
                </tr>
                <tr>
                    <th>Age</th>
                    <td><input type="number" name="age" class="input-field" value="<%= age %>"></td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td><input type="email" name="email" class="input-field" value="<%= email %>"></td>
                </tr>
            </table>
            <button type="submit" class="edit-button">Edit Info</button>
        </form>
    </div>
</body>
</html>